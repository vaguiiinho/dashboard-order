# Backend Environment Configuration Example
# Copy this file to .env and update the values as needed

# Database Configuration
# IMPORTANTE: Dentro do Docker, use o nome do serviço 'mysql' como host
# Para desenvolvimento local (fora do Docker), use 'localhost'
DATABASE_URL="mysql://root:rootpassword@mysql:3306/dashboard_order_db"

# Application Configuration
PORT=3001
NODE_ENV=production
# FRONTEND_URL pode ser * para permitir qualquer origem ou específica
FRONTEND_URL=*

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d


